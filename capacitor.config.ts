import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.usd286.sedanbehaviortracker',
  appName: 'Sedan Behavior Tracker',
  webDir: 'build'
};

export default config;
